version: 1

on:
  push:
    deployments:
      - target: dev
        cwd: /opt/elera-example
        commands:
          - git pull --ff-only
          - npm ci
          - npm test
          - npm run lint
          - npm run check
          - npm run audit
          - npm run pack
